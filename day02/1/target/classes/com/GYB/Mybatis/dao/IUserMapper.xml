<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.GYB.Mybatis.dao.IUserDao">
    <select id="findAll" resultType="com.GYB.Mybatis.domain.User">
        select * from user;
    </select>
    <insert id="save" parameterType="com.GYB.Mybatis.domain.User">
        insert into user(username,birthday,address,sex)values(#{username},#{birthday},#{address},#{sex});
    </insert>
    <update id="upDate" parameterType="com.GYB.Mybatis.dao.IUserDao">
        update user set username=#{username},address=#{address},birthday=#{birthday},sex=#{sex} where id= #{id};
    </update>
</mapper>