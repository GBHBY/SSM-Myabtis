<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

    <properties resource="jdbcConfig.properties">
        <!-- <property name="driver" value="com.mysql.jdbc.Driver"/>
            <property name="url" value="jdbc:mysql://localhost:3306/mybatis?serverTimezone=UTC&amp;characterEncoding=UTF8"/>
            <property name="username" value="root"/>
            <property name="password" value="123456"/>-->
    </properties>
    <typeAliases>
        <!--  <typeAlias type="com.GYB.Mybatis.domain.User" alias="user"></typeAlias>-->
        <!--这个标签，用于指定要配置别名的包，一旦指定，该包下所有的实体类都会注册别名，并且类名就是别名，不再区分大小写-->
        <package name="com.GYB.Mybatis.domain"/>
    </typeAliases>

    <environments default="mysql">
        <environment id="mysql">
            <transactionManager type="JDBC"></transactionManager>

            <dataSource type="POOLED">
                <property name="driver" value="${jdbc.driver}"/>
                <property name="url" value="${jdbc.url}"/>
                <property name="username" value="${jdbc.username}"/>
                <property name="password" value="${jdbc.password}"/>
            </dataSource>

        </environment>
    </environments>
    <mappers>

       <!-- <mapper resource="com/GYB/Mybatis/dao/IUserDao.xml"></mapper>-->
        <!--这个package标签用于指定dao接口所在的包，当指定完成之后，就不需要再写 mapper的那些属性了-->
        <package name="com.GYB.Mybatis.dao"/>
    </mappers>
</configuration>
