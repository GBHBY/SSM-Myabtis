<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.GYB.Mybatis.dao.IRoleDao">
    <!--定义resultMap，因为Java和数据库的命名规则不同 property对应的是实体类中的属性，Colum对应的是字段名-->
    <resultMap id="roleMap" type="role">
        <id property="roleId" column="rid"/>
        <result property="roleName" column="role_name"/>
        <result property="roleDesc" column="role_desc"/>
        <collection property="users" ofType="user">
            <id property="id" column="id"/>
            <result property="sex" column="sex"/>
            <result property="birthday" column="birthday"/>
            <result property="address" column="address"/>
            <result property="username" column="username"/>
        </collection>
    </resultMap>

    <!--查询所有-->
    <select id="findAll" resultMap="roleMap">
      select u.*,r.id as rid ,r.ROLE_NAME,r.ROLE_DESC from role r left outer join user_role ur on r.id = ur.RID left outer
        join user u on u.id = ur.uid


    </select>

</mapper>