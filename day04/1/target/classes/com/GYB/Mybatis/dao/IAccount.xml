<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.GYB.Mybatis.dao.IAccount">
    <!--定义封装account和user的resultMap-->
    <resultMap id="accountUserMap" type="account">
        <id property="id" column="id"/>
        <result property="uid" column="uid"/>
        <result property="money" column="money"/>
        <!--select属性指定的内容和：查询用户的唯一标识，也就是用户的id-->
        <!--column的属性内容：因为要用到的是findById方法，参数就是column的值-->
        <association property="user" column="uid" select="com.GYB.Mybatis.dao.IUserDao.findById">

        </association>


    </resultMap>


    <select id="findAllByUserId" resultType="account" parameterType="int">
        select * from account where  uid = #{id}
    </select>

    <select id="findAll" resultMap="accountUserMap">
        select *  from account;
    </select>

</mapper>